<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<artifactId>maltcms-chroma4D-srg-module</artifactId>
		<groupId>net.sf.maltcms</groupId>
		<version>2.0-SNAPSHOT</version>
		<relativePath>../</relativePath>
	</parent>
	<groupId>net.sf.maltcms</groupId>
	<artifactId>maltcms-chroma4D-srg-dist</artifactId>
	<name>maltcms-chroma4D-srg-dist</name>
	<version>2.0-SNAPSHOT</version>
	<packaging>pom</packaging>
	<build>
		<plugins><!-- Dependency plugin -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-dependency-plugin</artifactId>
				<version>2.0</version>
				<executions>
					<execution>
						<id>copy-dependencies</id>
						<phase>package</phase>
						<goals>
							<goal>copy-dependencies</goal>
						</goals>
						<configuration>
							<outputDirectory>${project.build.directory}/${project.name}-${project.version}/lib</outputDirectory>
							<overWriteReleases>true</overWriteReleases>
							<overWriteSnapshots>true</overWriteSnapshots>
							<overWriteIfNewer>true</overWriteIfNewer>
						</configuration>
					</execution>
					<execution>
						<id>unpack</id>
						<phase>package</phase>
						<goals>
							<goal>unpack</goal>
						</goals>
						<configuration>
							<artifactItems>
								<artifactItem>
									<groupId>net.sf.maltcms</groupId>
									<artifactId>maltcms-resources</artifactId>
									<type>jar</type>
									<overWrite>true</overWrite>
									<outputDirectory>${project.build.directory}/${project.name}-${project.version}</outputDirectory>
									<!--<destFileName>optional-new-name.jar</destFileName> -->
									<!--<includes>**/*.class,**/*.xml</includes> -->
									<excludes>META-INF/</excludes>
								</artifactItem>
								<artifactItem>
									<groupId>net.sf.maltcms</groupId>
									<artifactId>maltcms-chroma4D-srg</artifactId>
									<type>jar</type>
									<overWrite>true</overWrite>
									<outputDirectory>${project.build.directory}/${project.name}-${project.version}</outputDirectory>
									<includes>cfg/</includes>
									<excludes>META-INF/</excludes>
								</artifactItem>
								<artifactItem>
									<groupId>net.sf.maltcms</groupId>
									<artifactId>maltcms-chroma4D-cwt</artifactId>
									<type>jar</type>
									<overWrite>true</overWrite>
									<outputDirectory>${project.build.directory}/${project.name}-${project.version}</outputDirectory>
									<includes>cfg/</includes>
									<excludes>META-INF/</excludes>
								</artifactItem>
							</artifactItems>
							<!--<includes>**/*.java</includes> -->
							<excludes>META-INF/</excludes>
							<outputDirectory>${project.build.directory}/${project.name}-${project.version}</outputDirectory>
							<overWriteReleases>true</overWriteReleases>
							<overWriteSnapshots>true</overWriteSnapshots>
							<overWriteIfNewer>true</overWriteIfNewer>
						</configuration>
					</execution>

				</executions>
			</plugin>

			<!-- Jar plugin -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-jar-plugin</artifactId>
				<version>2.2</version>
				<executions>
					<execution>
						<id>create-cp-jar</id>
						<phase>package</phase>
						<goals>
							<goal>jar</goal>
						</goals>
						<configuration>
							<archive>
								<manifest>
									<mainClass>net.sf.maltcms.apps.Maltcms</mainClass>
									<addClasspath>true</addClasspath>
									<classpathPrefix>lib</classpathPrefix>
								</manifest>
							</archive>
							<finalName>maltcms</finalName>
							<outputDirectory>${project.build.directory}/${project.name}-${project.version}</outputDirectory>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-assembly-plugin</artifactId>
				<version>2.2-beta-2</version>
<!--				<dependencies>
					<dependency>
						<groupId>net.sf.maltcms</groupId>
						<artifactId>maltcms</artifactId>
						<version>1.0.6-SNAPSHOT</version>
						<type>pom</type>
					</dependency>
				</dependencies>
-->
				<executions>
					<execution>
						<id>my-assembly</id>
						<phase>package</phase>
						<goals>
							<goal>single</goal>
						</goals>
					</execution>
				</executions>

				<configuration>
					<filters>
						<filter>src/assemble/filter.properties</filter>
					</filters>
					<descriptors>
						<descriptor>src/assemble/distribution.xml</descriptor>
					</descriptors>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-antrun-plugin</artifactId>
				<version>1.3</version>
				<executions>
					<execution>
						<id>set-file-perms</id>
						<phase>package</phase>
						<configuration>
							<tasks>
								<echo
									message="Correcting file permissions in ${project.build.directory}/${project.name}-${project.version}/scripts/" />
								<chmod includes="*.sh,*.bat"
									dir="${project.build.directory}/${project.name}-${project.version}/scripts/"
									perm="ugo+rx" />
							</tasks>
						</configuration>
						<goals>
							<goal>run</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>
	<dependencies>
		<dependency>
			<groupId>net.sf.maltcms</groupId>
			<artifactId>maltcms-chroma4D-jbl</artifactId>
			<version>2.0-SNAPSHOT</version>
		</dependency>
		<dependency>
			<groupId>net.sf.maltcms</groupId>
			<artifactId>maltcms-chroma4D-cwt</artifactId>
			<version>2.0-SNAPSHOT</version>
		</dependency>
		<dependency>
			<groupId>net.sf.maltcms</groupId>
			<artifactId>maltcms-chroma4D-srg</artifactId>
			<version>2.0-SNAPSHOT</version>
		</dependency>
	</dependencies>
</project>
