#Adding the Id property recursively for newly added files: 
svn propset svn:keywords "Id" -R *

#Preparing a release:
#check that all files have a license header:
mvn license:check

#if any files lack a license header, fix with:
mvn license:format
#call release prepare
mvn release:prepare -Dusername=[username] -Dpassword=[password]
#call release perform
mvn release:perform
