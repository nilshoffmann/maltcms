<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <artifactId>maltcms-distributions</artifactId>
        <groupId>io.github.nilshoffmann</groupId>
        <version>2.0.0-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>
    <artifactId>maltcms-nbm-cluster</artifactId>
    <name>Maltcms Distribution NBM</name>
    <description>Creates the Maltcms NetBeans Module (NBM) Cluster.</description>
    <packaging>pom</packaging>

    <profiles>
        <profile>
            <id>nbm</id>
            <modules>
                <module>db4o-nbm</module>
                <module>jai-nbm</module>
                <module>jfreechart-nbm</module>
                <module>maltcms-nbm</module>
                <module>groovy-nbm</module>
            </modules>
            <build> 
                <pluginManagement>
                    <plugins>
                        <plugin>
                            <groupId>org.apache.netbeans.utilities</groupId>
                            <artifactId>nbm-maven-plugin</artifactId>
                            <extensions>true</extensions>
                            <version>4.8</version>
                        </plugin>
                        <plugin>
                            <groupId>org.apache.maven.plugins</groupId>
                            <artifactId>maven-jar-plugin</artifactId>
                            <version>3.3.0</version>
                            <configuration>
                                <archive>
                                    <manifestFile>${project.build.outputDirectory}/META-INF/MANIFEST.MF</manifestFile>
                                </archive>
                            </configuration>
                        </plugin>
                    </plugins>
                </pluginManagement>
                <plugins>
                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>nbm-maven-plugin</artifactId>
                        <extensions>true</extensions>
                        <executions>
                            <execution>
                                <id>extra</id>
                                <goals>
                                    <goal>cluster</goal>
                                    <goal>autoupdate</goal>
                                </goals>
                            </execution>
                        </executions>
                        <configuration>
                            <verifyRuntime>warn</verifyRuntime>
                            <outputDirectory>${nbm.updatecenter.outputDir}</outputDirectory>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
    <modules>
        <module>maltcms-nbm</module>
        <module>jfreechart-nbm</module>
        <module>jai-nbm</module>
        <module>groovy-nbm</module>
        <module>db4o-nbm</module>
    </modules>
    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.netbeans.api</groupId>
                <artifactId>org-openide-util-lookup</artifactId>
                <version>${version.netbeans}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>
    <properties>
        <version.netbeans>RELEASE180</version.netbeans>
        <nbm.updatecenter.outputDir>${project.build.directory}</nbm.updatecenter.outputDir>
    </properties>
    
</project>
